# ibmi-agentic-data

Example data for [ibmi-agentic](https://github.com/ProfoundLogic/ibmi-agentic) repo.

## Populating Test Data

The `ibmi-agentic` repo base environment must be built first.

To clear/populate the example data:

```bash
export BUILD_LIBRARY=XXAIBASE
make
```
